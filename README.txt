Downloaded the google tesseract from google with the ubuntu software package manager

downloaded the pdf2image software

Needed to install poppler-utils for the pdf to image software

pip install scikit-build for the opencv-python

also needed pip install cmake

pip install opencv-contrib-python
